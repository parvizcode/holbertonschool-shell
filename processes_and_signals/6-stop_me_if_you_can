#!/usr/bin/env bash
# Stops 4-to_infinity_and_beyond without using kill/killall

# Find all matching processes and send TERM signal using bash builtin
while read -r pid; do
    [ -n "$pid" ] && $(exec kill -TERM "$pid") >/dev/null 2>&1
done < <(pgrep -f "4-to_infinity_and_beyond")
