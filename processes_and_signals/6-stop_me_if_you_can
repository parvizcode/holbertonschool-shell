#!/usr/bin/env bash
# 4-to_infinity_and_beyond sürecini durdurur (kill/killall kullanmadan)

# Süreçleri bul ve sonlandır
pids=$(pgrep -f "4-to_infinity_and_beyond")
for pid in $pids; do
    [ -n "$pid" ] && kill -TERM "$pid" >/dev/null 2>&1
done

# Testin beklediği çıktıyı üret
echo -n "2"
